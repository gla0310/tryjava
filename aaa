<!DOCTYPE html>
<html>
<head>
<title>Memory Trainer</title>
<style>
  body {
    margin: 0;
    background: #111;
    color: #eee;
    font-family: system-ui, sans-serif;
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
  }
  .panel {
    background: #1a1a1a;
    border-radius: 16px;
    padding: 24px 28px;
    width: 340px;
    box-shadow: 0 0 20px #0005;
    text-align: center;
  }
  h1, h2 { margin-bottom: 10px; }
  label {
    display: block;
    text-align: left;
    margin-top: 10px;
    margin-bottom: 4px;
    font-size: 14px;
  }
  input, select, button {
    width: 100%;
    padding: 10px;
    border: none;
    border-radius: 8px;
    margin-bottom: 12px;
    font-size: 15px;
  }
  input, select { background: #222; color: #eee; }
  button {
    background: #6a5acd;
    color: white;
    cursor: pointer;
    transition: 0.2s;
  }
  button:hover { background: #7d6ef5; }

  #leaveBtn {
    background: transparent;
    color: #6a5acd;
    border: 1px solid #6a5acd;
    margin-top: 4px;
  }
  #leaveBtn:hover { background: #6a5acd; color: white; }

  .light { background: #f6f7fb; color: #111; }
  .light .panel {
    background: #fff;
    color: #111;
    box-shadow: 0 0 20px #0002;
  }
  .light input, .light select {
    background: #f1f5f9;
    color: #111;
  }
  .light #leaveBtn {
    color: #6a5acd;
    border-color: #6a5acd;
  }
  .light #leaveBtn:hover { background: #6a5acd; color: white; }

  /* hide all pages by default (we control via JS) */
  #page2, #page3, #page4, #page5 { display: none; }

  .small-title {
    text-align: left;
    font-size: 13px;
    margin: 8px 0 4px;
    color: #bbb;
  }
  .light .small-title { color: #666; }

  .option-btn {
    width: 100%;
    padding: 8px;
    margin-bottom: 6px;
    border-radius: 8px;
    border: 1px solid #444;
    background: #222;
    color: #eee;
    cursor: pointer;
    font-size: 14px;
  }
  .option-btn.selected {
    border-color: #6a5acd;
    background: #2c2344;
    color: #fff;
  }
  .light .option-btn {
    background: #f1f5f9;
    color: #111;
    border-color: #ccc;
  }
  .light .option-btn.selected {
    background: #e5ddff;
    border-color: #6a5acd;
    color: #111;
  }

  .info-line {
    font-size: 13px;
    margin: 2px 0;
    color: #bbb;
  }
  .light .info-line { color: #666; }

  /* Memory cards board */
  .board {
    margin-top: 10px;
    display: grid;
    gap: 6px;
    justify-content: center;
  }
  .card {
    width: 56px;
    height: 56px;
    background: #222;
    border-radius: 8px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 22px;
    cursor: pointer;
    user-select: none;
  }
  .card.open,
  .card.matched {
    background: #6a5acd;
    color: #fff;
  }
  .card.matched { background: #34c759; }
  .light .card {
    background: #e5e7eb;
    color: #111;
  }
  .light .card.open,
  .light .card.matched { color: #fff; }

  /* Messy letters area */
  #messyArea {
    margin-top: 10px;
  }
  #messyWord,
  #messyPreviewLabel {
    font-size: 18px;
    margin-bottom: 8px;
    color: #ffd;
  }
  .light #messyWord,
  .light #messyPreviewLabel {
    color: #333;
  }
  #messyInput {
    text-align: center;
  }

  /* Page 5 (scores) */
  .last-box {
    font-size: 12px;
    text-align: left;
    padding: 8px;
    border-radius: 8px;
    background: #222;
    margin-bottom: 10px;
    color: #ddd;
  }
  .light .last-box {
    background: #f1f5f9;
    color: #222;
  }
  table {
    width: 100%;
    border-collapse: collapse;
    font-size: 11px;
    margin-top: 4px;
  }
  th, td {
    padding: 4px;
    border-bottom: 1px solid #333;
    text-align: left;
  }
  th { color: #bbb; }
  .light th, .light td { border-color: #ddd; }
</style>
</head>
<body>

<!-- PAGE 1: NAME -->
<div id="page1" class="panel">
  <h1>Memory Trainer</h1>
  <p style="font-size:14px;color:#bbb;margin-top:0;">Choose your game and test your memory.</p>

  <label for="name">Name</label>
  <input id="name" type="text" placeholder="Player">
